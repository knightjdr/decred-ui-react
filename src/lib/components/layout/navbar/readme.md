<strong>Default</strong>

```jsx
const { BrowserRouter } = require('react-router-dom');
<BrowserRouter>
  <div class="navbar-wrapper">
    <Navbar
      background
      fixed={false}
      links={[
        { route: '/', text: 'POLITEIA'},
      ]}
      style={{ width: '95%' }}
    />
  </div>
</BrowserRouter>
```
